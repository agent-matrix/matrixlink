# ==== BFF / Front Door ====
ADMIN_BEARER=changeme-admin-bearer-token

# MCP Gateway (control plane)
MCP_BASE_URL=https://mcp-gateway.<project>.<region>.codeengine.appdomain.cloud
MCP_BEARER_TOKEN=<paste-mcp-service-jwt>

# Agents (optional static base for direct calls from BFF)
AGENTS_BASE_URL=https://agents.<project>.<region>.codeengine.appdomain.cloud
AGENTS_API_TOKEN=<paste-east-west-token>

# CORS (admin UI)
CORS_ALLOW_ORIGINS=http://localhost:3000

# ==== MatrixLink lib defaults ====
REQUEST_TIMEOUT=30
TENANT_HEADER=X-Tenant-Id
